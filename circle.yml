machine:
  environment:
    XCODE_WORKSPACE: 'tips.xcworkspace'
    XCODE_SCHEME: 'tips'
dependencies:
  pre:
    - gem install cocoapods --pre
    - pod install
test:
  override:
    - rake clean; rake test
